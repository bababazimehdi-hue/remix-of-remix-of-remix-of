# راهنمای زیرساخت بک‌اند و سینک - دوچرخه یار

## خلاصه اجرایی

بک‌اند این پروژه **فقط و فقط Supabase** است — بدون هیچ سرور جداگانه یا Edge
Function اضافه. هر تغییری که هرکسی روی هر دستگاهی انجام می‌دهد، بلافاصله
به دیتابیس مشترک Supabase نوشته می‌شود و از طریق Supabase Realtime به تمام
دستگاه‌های دیگر (چند گوشی همزمان) می‌رسد.

- ✅ سینک واقعی و آنی بین همه دستگاه‌ها (نه فقط اعلان‌ها — همه چیز: خریدها،
  هزینه‌ها، وظایف، فاکتورها، پیام‌ها، کاربران، تنظیمات)
- ✅ اعلان‌ها با احترام به «ساعات سکوت» تنظیم‌شده توسط ادمین
  (`AlarmSettings.startHour/endHour/roles`)
- ✅ نوشتن‌های ناموفق به‌خاطر قطعی موقت اینترنت، خودکار Retry می‌شوند و گم
  نمی‌شوند (حتی بعد از بستن/رفرش صفحه)
- ✅ بدون نیاز به هاست جداگانه؛ بدون Edge Function دده‌کد

## ساختار فایل‌های مرتبط با سینک

```
src/lib/
├── store.tsx      # State مرکزی + StoreProvider؛ نقطه‌ی واحد نوشتن (setState)
├── db.ts          # لایه‌ی نگاشت State <-> جداول Supabase + pushChanges() + subscribeAll()
└── offline-db.ts  # لایه‌ی کوچک IndexedDB؛ فقط برای نگه‌داشتن «یک نوشتن ناتمام»
                    # تا زمانی که واقعاً روی Supabase تأیید شود (محافظت در برابر
                    # قطعی اینترنت/بستن مرورگر وسط کار)
```

هیچ Edge Function و هیچ صف/موتور سینک جداگانه‌ای در این پروژه وجود ندارد؛
تمام منطق سینک همین سه فایل است.

## معماری واقعی سینک

### ۱. مسیر نوشتن: `setState` → `pushChanges` (مستقیم روی Supabase)

هر جای برنامه که چیزی تغییر می‌کند (`useStore().setState(...)`)، این اتفاق
می‌افتد:

1. State محلی فوراً به‌روزرسانی می‌شود (UI بدون تأخیر واکنش می‌دهد).
2. تفاوت بین state قبلی و جدید (`diff()` در `db.ts`) محاسبه می‌شود:
   ردیف‌های insert/update/soft-delete برای هرکدام از جدول‌های
   `bicycle_purchases`, `expenses`, `tasks`, `purchase_invoices`+`invoice_items`,
   `notifications`, `messages`, `app_settings`، و کاربران (از طریق تابع سرور
   محافظت‌شده‌ی `saveTeamUser`).
3. `pushChanges(prev, next, userId)` این تغییرات را روی Supabase می‌نویسد.
4. اگر بنویسد موفق شد → `refresh()` صدا زده می‌شود تا state محلی با آنچه واقعاً
   در دیتابیس هست هماهنگ (reconcile) شود.
5. اگر ناموفق شد و اینترنت وصل است → حداکثر ۵ بار با backoff نمایی (تا ۱۰ ثانیه)
   تلاش دوباره می‌شود؛ اگر باز هم ناموفق بود، خطا به‌صورت toast به کاربر نشان
   داده می‌شود.
6. اگر ناموفق شد به‌خاطر قطعی اینترنت → تلاش‌ها بی‌نهایت ادامه پیدا می‌کنند
   (منتظر رویداد `online` مرورگر یا حداکثر هر ۳۰ ثانیه یک بار) — یعنی هیچ
   داده‌ای در قطعی گم نمی‌شود.

پیش از هر تلاش برای نوشتن، دقیقاً همان `{base, next}` که باید نوشته شود در
IndexedDB (`saveState` در `offline-db.ts`) ذخیره می‌شود. اگر کاربر وسط تلاش
مجدد صفحه را ببندد یا رفرش کند، هنگام بالا آمدن دوباره‌ی برنامه
(`resumePendingPush` در `store.tsx`) همان نوشتن ناتمام از سر گرفته می‌شود —
نه از نو ساخته می‌شود، نه گم می‌شود.

### ۲. مسیر خواندن/همگام‌سازی زنده: Supabase Realtime (`subscribeAll`)

هر دستگاه پس از ورود، یک کانال Realtime واحد باز می‌کند که روی همه‌ی
جدول‌های مشترک (`SYNCED_TABLES` در `db.ts`) گوش می‌دهد. به محض این‌که هر
دستگاه دیگری تغییری بنویسد، همه‌ی دستگاه‌های دیگر در عرض چند صدم ثانیه
(coalesce ۱۲۰ میلی‌ثانیه‌ای برای دسته‌ای شدن رویدادها) یک `refresh()` انجام
می‌دهند و state خودشان را به‌روز می‌کنند.

این کانال به‌طور خودکار reconnect می‌شود (backoff نمایی تا ۱۵ ثانیه)، و با
برگشت اینترنت یا برگشت تب به حالت فعال، فوراً یک sync کامل انجام می‌دهد. این
بخش دست‌نخورده و از ابتدا سالم بوده است.

### ۳. اعلان‌ها و ساعات سکوت (Alarm/Quiet-Hours)

اعلان‌ها (`notify()` در `store.tsx`) از **همان مسیر** `setState → pushChanges`
عبور می‌کنند — یعنی یک منطق واحد، بدون کد تکراری. زمان تحویل (`deliverAt`) با
تابع `computeDeliverAt(alarms, roles, now)` محاسبه می‌شود که تنظیمات واقعی
ادمین را می‌خواند:

- `AlarmSettings.enabled` — کل سیستم ساعات سکوت را روشن/خاموش می‌کند.
- `AlarmSettings.startHour` / `endHour` — بازه‌ی ساعتی که آلارم فقط برای
  نقش‌های ذکرشده به تعویق می‌افتد.
- `AlarmSettings.roles` — این بازه فقط برای این نقش‌ها اعمال می‌شود؛ سایر
  نقش‌ها فوراً اعلان می‌گیرند.
- اعلان‌های `URGENT` همیشه فوری تحویل داده می‌شوند، مستقل از ساعات سکوت.
- هر `AlarmEventKey` (پیام جدید، پیام فوری، وظیفه جدید، …) هم می‌تواند از
  تنظیمات صفحه‌ی «آلارم‌ها» کاملاً خاموش شود یا سطح/صدا/ویبره‌ی خودش را داشته
  باشد (`AlarmSettings.events`).

## چرا نسخه‌ی قبلی (IndexedDB + SyncEngine + Edge Function) کار نمی‌کرد

یک نسخه‌ی قبلی این پروژه یک لایه‌ی «آفلاین-اول» جدید معرفی کرده بود
(`sync-engine.ts` + `notification-service.ts` + Edge Function
`supabase/functions/sync-batch`) که ادعا می‌کرد سینک را قوی‌تر می‌کند، اما در
عمل مسیر نوشتن اصلی (`pushChanges`) را به‌طور کامل جایگزین کرده بود با یک صف
IndexedDB که **هیچ‌وقت برای داده‌های تجاری واقعی (خریدها، هزینه‌ها، وظایف،
فاکتورها، پیام‌ها، کاربران) پر نمی‌شد** — فقط اعلان‌ها از مسیر جدید و ناقص
عبور می‌کردند. نتیجه: تمام تغییرات به‌جز اعلان‌ها فقط در حافظه‌ی مرورگر همان
دستگاه باقی می‌ماندند و هیچ‌وقت به Supabase نمی‌رسیدند، یعنی سینک چند‌دستگاهی
عملاً از کار افتاده بود. علاوه بر این:

- `NotificationService.computeDeliverAt()` ساعات سکوت را به‌صورت ثابت
  (۲۳:۰۰–۰۷:۰۰) کدنویسی کرده بود و تنظیمات واقعی ادمین را نادیده می‌گرفت.
- Edge Function `sync-batch` هرگز از سمت کلاینت صدا زده نمی‌شد (کد مرده) و به
  جدولی (`sync_activity_log`) اشاره می‌کرد که هیچ migration ای برایش وجود
  نداشت.
- فایل `.gitignore` هم در همان تغییر خراب شده بود (به‌صورت یک بلاک Markdown
  با ```` ``` ```` دور آن) که باعث می‌شد صدها فایل build خروجی (`.output/`,
  `.wrangler/`) به‌اشتباه در گیت ردیابی شوند.

همه‌ی این موارد در این نسخه اصلاح شده‌اند: کد اضافه‌ی بلااستفاده حذف شده، مسیر
اصلی نوشتن به `pushChanges` برگشته (با یک لایه‌ی retry/resume واقعی و سبک روی
آن)، ساعات سکوت به تنظیمات واقعی ادمین متصل شده، و `.gitignore` تعمیر شده است.

## چک‌لیست تأیید سینک

- دو (یا چند) دستگاه را با حساب‌های مختلف در همان فروشگاه وارد کنید.
- روی یک دستگاه یک خرید/هزینه/وظیفه/پیام جدید ثبت کنید.
- در عرض کسری از ثانیه (بدون رفرش دستی) باید در دستگاه دیگر ظاهر شود؛ اگر
  ظاهر نشد، نشانگر اتصال کنار زنگوله‌ی اعلان‌ها را چک کنید («تلاش دوباره»).
- برای تست حالت آفلاین: یک دستگاه را در حالت هواپیما بگذارید، چیزی ثبت کنید،
  صفحه را ببندید، دوباره باز کنید و اینترنت را وصل کنید — تغییر باید بدون
  نیاز به تکرار عمل، خودش روی سرور بنشیند.
